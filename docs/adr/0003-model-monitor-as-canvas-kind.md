# Model Monitor as a Canvas Kind

Monitor is modeled as a canvas kind inside the Canvas Workspace model, not as a separate workspace mode. This keeps the default operating view, workflow boards, subcanvas drilldown, recap, and evidence on one product axis; the trade-off is that the canvas layer now owns monitor-specific rendering instead of letting a standalone monitor page evolve independently.
