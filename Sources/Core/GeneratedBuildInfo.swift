// Placeholder overwritten by scripts/gen-build-info.sh for local/release builds.
// Keep this committed copy stable so a plain `swift build` always compiles.
enum GeneratedBuildInfo {
    static let gitCommit = "unknown"
    static let gitBranch = "unknown"
    static let buildDate = "unknown"
}
